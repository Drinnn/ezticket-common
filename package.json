{
  "name": "ezticket-common",
  "version": "1.0.0",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "yarn clean && tsc",
    "publish": "npm publish"
  },
  "license": "MIT",
  "devDependencies": {
    "del-cli": "^5.0.0",
    "typescript": "^4.7.4"
  }
}
